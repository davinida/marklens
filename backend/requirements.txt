# MarkLens backend 추가 패키지
#
# 본 백엔드는 ml/venv 를 공유합니다.
# ML 관련 패키지(torch, faiss-cpu, open_clip_torch, pillow, numpy)는
# ml/requirements.txt 에서 관리되며 여기에 중복 기재하지 않습니다.
#
# 이 파일에는 웹 API 계층에서만 새로 필요한 의존성만 기록합니다.

# 웹 프레임워크 (pydantic 자동 동반)
fastapi>=0.115.0,<1.0.0

# ASGI 서버 (standard extras: httptools, watchfiles, uvloop, websockets 포함)
uvicorn[standard]>=0.32.0,<1.0.0

# multipart/form-data 파일 업로드 처리 (FastAPI가 별도로 요구)
python-multipart>=0.0.20,<1.0.0

# .env 로드 (KIPRIS 키/DATABASE_URL — 키 커밋 금지 정책의 실행 수단)
python-dotenv>=1.0.0,<2.0.0

# KIPRIS Open API 호출 (백엔드-5 수집 / 백엔드-7 호칭 검색)
httpx>=0.27.0,<1.0.0

# PostgreSQL 드라이버 + 커넥션 풀 (백엔드-1~4)
# DATABASE_URL 미설정이면 서버는 기존 file 모드로 동작하므로 DB가 없어도 무방
psycopg[binary]>=3.1.0,<4.0.0
psycopg-pool>=3.2.0,<4.0.0

# /name-check 질의 캐시 (상한 + TTL — KIPRIS 월 호출 한도 보호 장치)
cachetools>=5.3.0,<7.0.0
