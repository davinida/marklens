# 개발/테스트 전용 의존성 (배포 불필요)
# 설치: ml/venv 에 함께 설치
#   ml\venv\Scripts\python.exe -m pip install -r backend\requirements-dev.txt

# 테스트 러너 (backend/tests)
pytest>=9.0.3,<10.0.0

# Starlette 1.x TestClient runtime. 운영 KIPRIS 클라이언트의 httpx와 별도 설치한다.
httpx2>=2.9.0,<3.0.0

# 부하 베이스라인 (감사보고서 R13 — backend/tests/perf/locustfile.py)
# 다축 확장 전 /search·/health 성능 기준점 측정용. 측정 절차·기록 템플릿은
# docs/MarkLens_부하테스트_베이스라인.md 참조. (설치는 측정할 때만 — 상시 불요)
locust>=2.30.0,<3.0.0

# CI 정적 검사와 알려진 취약점 감사
ruff>=0.16.3,<0.17.0
pip-audit>=2.10.1,<3.0.0
